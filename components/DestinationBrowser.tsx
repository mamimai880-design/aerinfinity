"use client";
import { useState } from "react";
import { Search } from "lucide-react";
import { destinations } from "@/data/destinations";
import DestinationCard from "./DestinationCard";
export default function DestinationBrowser() { const [query, setQuery] = useState(""); const [region, setRegion] = useState("All"); const regions = ["All", "North America", "Europe", "Asia", "Oceania", "Africa"]; const shown = destinations.filter((item) => (region === "All" || item.region === region) && `${item.city} ${item.code} ${item.country}`.toLowerCase().includes(query.toLowerCase())); return <><div className="mb-8 flex flex-col gap-3 md:flex-row"><label className="flex flex-1 items-center gap-3 border border-white/15 bg-[#0b1d33] px-4 py-3 text-[#95a9bf]"><Search size={16} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search cities, airports, codes" className="w-full bg-transparent text-sm outline-none placeholder:text-[#60758d]" /></label><select value={region} onChange={(event) => setRegion(event.target.value)} className="border border-white/15 bg-[#0b1d33] px-4 py-3 text-xs font-bold uppercase tracking-widest text-[#95a9bf] outline-none">{regions.map((item) => <option key={item}>{item}</option>)}</select></div><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{shown.map((item) => <DestinationCard key={item.code} item={item} />)}</div></>; }