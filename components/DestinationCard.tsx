import Image from "next/image";
import type { Destination } from "@/data/destinations";
export default function DestinationCard({ item }: { item: Destination }) { return <article className="group relative min-h-[270px] overflow-hidden border border-white/10"><Image src={item.image} alt={`${item.city}, ${item.country}`} fill sizes="(max-width: 768px) 100vw, 33vw" className="object-cover transition duration-700 group-hover:scale-105" /><div className="absolute inset-0 bg-gradient-to-t from-[#06111f] via-[#06111f]/30 to-transparent" /><div className="absolute inset-x-0 bottom-0 p-5"><div className="flex items-end justify-between"><div><p className="display-font text-4xl">{item.code}</p><p className="mt-1 text-lg font-semibold">{item.city}</p><p className="text-xs text-[#b4c5d8]">{item.airport}</p></div>{item.hub && <span className="mb-1 border border-[#54d5ff]/50 px-2 py-1 text-[9px] font-bold uppercase tracking-widest text-[#54d5ff]">AI hub</span>}</div></div></article>; }