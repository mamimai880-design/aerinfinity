import { Radio, ArrowRight } from "lucide-react";
import { flights } from "@/data/flights";
export default function FlightTracker() { return <div className="glass grid divide-y divide-white/10 md:grid-cols-3 md:divide-x md:divide-y-0">{flights.map((flight) => <div key={flight.number} className="p-5"><div className="mb-8 flex items-center justify-between"><span className="eyebrow">{flight.number}</span><span className="flex items-center gap-2 text-[10px] uppercase tracking-widest text-[#54d5ff]"><Radio size={12} /> {flight.status}</span></div><p className="display-font text-3xl">{flight.route}</p><div className="mt-4 flex items-center gap-3 text-xs text-[#95a9bf]"><span>{flight.aircraft}</span><ArrowRight size={13} className="text-[#54d5ff]" /><span>{flight.detail}</span></div></div>)}</div>; }