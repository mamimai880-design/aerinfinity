import Image from "next/image";
import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import type { Aircraft } from "@/data/aircraft";
export default function AircraftCard({ item }: { item: Aircraft }) { return <Link href={`/fleet/${item.slug}`} className="group block border border-white/10 bg-[#0b1d33] transition hover:-translate-y-1 hover:border-[#54d5ff]/50"><div className="relative aspect-[1.5] overflow-hidden"><Image src={item.image} alt={`${item.name} aircraft`} fill sizes="(max-width: 768px) 100vw, 33vw" className="object-cover transition duration-700 group-hover:scale-105" /><div className="absolute left-4 top-4 bg-[#06111f]/80 px-2 py-1 text-[10px] font-bold uppercase tracking-widest text-[#54d5ff]">{item.status}</div></div><div className="p-5"><div className="flex items-start justify-between"><div><p className="eyebrow">{item.manufacturer} · {item.family}</p><h3 className="mt-2 text-xl font-semibold">{item.name}</h3></div><ArrowUpRight className="text-[#54d5ff]" size={19} /></div><div className="mt-6 grid grid-cols-2 gap-y-3 text-xs text-[#95a9bf]"><span><b className="block text-[10px] uppercase tracking-widest text-[#5c7189]">Capacity</b>{item.capacity}</span><span><b className="block text-[10px] uppercase tracking-widest text-[#5c7189]">Range</b>{item.range}</span><span><b className="block text-[10px] uppercase tracking-widest text-[#5c7189]">Cruise</b>{item.speed}</span><span><b className="block text-[10px] uppercase tracking-widest text-[#5c7189]">Role</b>{item.role}</span></div></div></Link>; }